                <p style="font-size:10px;">@User.Identity.Name</p>
            @{
                for (int i = 0; i < Model.Names.Count; i++)
                {
                    (Model.Names[i] as String).ToLower();
                        }
            }
